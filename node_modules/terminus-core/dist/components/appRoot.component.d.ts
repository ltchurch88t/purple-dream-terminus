import { ElectronService } from '../services/electron.service';
import { HostAppService, Platform } from '../services/hostApp.service';
import { HotkeysService } from '../services/hotkeys.service';
import { LogService } from '../services/log.service';
import { QuitterService } from '../services/quitter.service';
import { ConfigService } from '../services/config.service';
import { DockingService } from '../services/docking.service';
import { TabRecoveryService } from '../services/tabRecovery.service';
import { ThemesService } from '../services/themes.service';
import { AppService, IToolbarButton, ToolbarButtonProvider } from '../api';
export declare class AppRootComponent {
    private docking;
    private electron;
    private tabRecovery;
    hostApp: HostAppService;
    hotkeys: HotkeysService;
    config: ConfigService;
    app: AppService;
    private toolbarButtonProviders;
    Platform: typeof Platform;
    ready: boolean;
    leftToolbarButtons: IToolbarButton[];
    rightToolbarButtons: IToolbarButton[];
    private logger;
    constructor(docking: DockingService, electron: ElectronService, tabRecovery: TabRecoveryService, hostApp: HostAppService, hotkeys: HotkeysService, config: ConfigService, app: AppService, toolbarButtonProviders: ToolbarButtonProvider[], log: LogService, _themes: ThemesService, _quitter: QuitterService);
    onGlobalHotkey(): void;
    ngOnInit(): Promise<void>;
    private getToolbarButtons(aboveZero);
}
