/// <reference types="electron" />
import { NgZone, EventEmitter } from '@angular/core';
import { ElectronService } from '../services/electron.service';
import { LogService } from '../services/log.service';
export declare enum Platform {
    Linux = 0,
    macOS = 1,
    Windows = 2,
}
export declare class HostAppService {
    private zone;
    private electron;
    platform: Platform;
    nodePlatform: string;
    quitRequested: EventEmitter<any>;
    ready: EventEmitter<any>;
    shown: EventEmitter<any>;
    secondInstance: EventEmitter<any>;
    private logger;
    constructor(zone: NgZone, electron: ElectronService, log: LogService);
    getWindow(): any;
    getShell(): any;
    getAppPath(): any;
    getPath(type: string): any;
    openDevTools(): void;
    setCloseable(flag: boolean): void;
    focusWindow(): void;
    toggleWindow(): void;
    minimize(): void;
    maximize(): void;
    unmaximize(): void;
    toggleMaximize(): void;
    setBounds(bounds: Electron.Rectangle): void;
    setAlwaysOnTop(flag: boolean): void;
    quit(): void;
}
