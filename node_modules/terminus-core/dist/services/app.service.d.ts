import { Subject } from 'rxjs';
import { ComponentFactoryResolver, Injector } from '@angular/core';
import { DefaultTabProvider } from '../api/defaultTabProvider';
import { BaseTabComponent } from '../components/baseTab.component';
import { Logger, LogService } from '../services/log.service';
export declare type TabComponentType = new (...args: any[]) => BaseTabComponent;
export declare class AppService {
    private componentFactoryResolver;
    private defaultTabProvider;
    private injector;
    tabs: BaseTabComponent[];
    activeTab: BaseTabComponent;
    lastTabIndex: number;
    logger: Logger;
    tabsChanged$: Subject<{}>;
    constructor(componentFactoryResolver: ComponentFactoryResolver, defaultTabProvider: DefaultTabProvider, injector: Injector, log: LogService);
    openNewTab(type: TabComponentType, inputs?: any): BaseTabComponent;
    openDefaultTab(): void;
    selectTab(tab: BaseTabComponent): void;
    toggleLastTab(): void;
    nextTab(): void;
    previousTab(): void;
    closeTab(tab: BaseTabComponent): void;
}
